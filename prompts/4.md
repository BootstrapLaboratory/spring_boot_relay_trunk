okay. lets improve my project even more. i know spring support something called live reload. so when application is running and i edit files, so it will update application in runtime... so lets do this. and I want it work in both microservices at same time
